{
  "name": "root",
  "private": true,
  "version": "1.0.0",
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "start": "lerna run start --scope={client-app,server-api}",
    "new-version": "lerna version --conventional-commits --yes",
    "diff": "lerna diff"
  }
}
